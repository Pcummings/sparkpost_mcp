---
phase: 01-test-foundation-ci
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "A GitHub Actions workflow at .github/workflows/ci.yml runs on push and pull_request"
    - "The job runs npm ci → npm run typecheck → npm test on a single Node LTS (22)"
    - "No SPARKPOST_API_KEY secret is referenced — tests are fully mocked (D-07)"
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "The workflow invokes the npm test script created in 01-01 (node:test via tsx)"
    - "actions/setup-node@v4 with cache: npm caches ~/.npm; npm ci consumes the lockfile"
---

<objective>
Add a GitHub Actions workflow (.github/workflows/ci.yml) that runs install → typecheck → test on every push and pull request, on a single Node LTS, with no secrets. Covers TEST-03.

Purpose: Make the test net automatic — every push and PR is gated on typecheck + tests so a broken request contract can't reach main unnoticed.
Output: .github/workflows/ci.yml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-foundation-ci/01-RESEARCH.md
@.planning/phases/01-test-foundation-ci/01-PATTERNS.md
@.planning/phases/01-test-foundation-ci/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol / File | Kind | Notes |
|---|---|---|
| `.github/workflows/ci.yml` | new CI workflow | Job `test`: checkout → setup-node (22, npm cache) → npm ci → npm run typecheck → npm test, on push + pull_request |

Consumes the npm `test` script and `typecheck` script (both exist after 01-01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub Actions CI workflow (install → typecheck → test)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-test-foundation-ci/01-RESEARCH.md — "GitHub Actions CI (TEST-03)" code example (the verified workflow YAML) and its Notes (Node 22 LTS pin, cache: npm, no secret needed, push + pull_request triggers).
    - .planning/phases/01-test-foundation-ci/01-PATTERNS.md — ".github/workflows/ci.yml" section (same verified template).
    - .planning/phases/01-test-foundation-ci/01-01-SUMMARY.md — confirm the npm `test` and `typecheck` script names exist as expected (CI invokes them by name).
  </read_first>
  <action>
    Create .github/workflows/ci.yml (create the .github/workflows/ directory; neither exists yet). Use the machine-verified template from RESEARCH verbatim:
    - `name: CI`
    - Triggers: `on: push:` and `pull_request:` with no branch filter (per D-06 — push + PR on all branches).
    - One job `test` on `runs-on: ubuntu-latest` with steps, in order:
      1. `uses: actions/checkout@v4`
      2. `uses: actions/setup-node@v4` with `node-version: "22"` and `cache: "npm"` (single Node LTS per D-07; cache: npm caches ~/.npm via setup-node, no extra config).
      3. `run: npm ci`
      4. `run: npm run typecheck`
      5. `run: npm test`
    Do NOT add any `env:` block, `secrets.SPARKPOST_API_KEY`, or a Node version matrix — tests are fully mocked (D-07), so CI needs no SparkPost secret and no matrix.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const must=['npm ci','npm run typecheck','npm test','actions/checkout@v4','actions/setup-node@v4','pull_request']; const missing=must.filter(s=>!y.includes(s)); if(missing.length) throw new Error('ci.yml missing: '+missing.join(', ')); if(/SPARKPOST_API_KEY/.test(y)) throw new Error('ci.yml must not reference SPARKPOST_API_KEY'); console.log('ci.yml OK');"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and triggers on both push and pull_request.
    - The job runs npm ci, then npm run typecheck, then npm test, in that order, on Node 22 with npm cache.
    - No SPARKPOST_API_KEY secret and no version matrix appear in the file.
    - The verify command prints "ci.yml OK".
  </acceptance_criteria>
  <done>.github/workflows/ci.yml gates push and PR on install → typecheck → test with a single Node LTS and no secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository secrets | The workflow references no secrets; the test suite is fully mocked, so no `SPARKPOST_API_KEY` is needed or exposed in Actions. |
| CI logs → public visibility | With no secret in the job, nothing sensitive can leak into Actions logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-CI-IDISC | Information Disclosure | .github/workflows/ci.yml | low | mitigate | No `SPARKPOST_API_KEY` secret in the workflow (D-07); tests monkeypatch `globalThis.fetch`, so CI never holds or logs a real key. |
| T-01-SC | Tampering | actions/* + npm ci | low | accept | Action versions are pinned to major `@v4` and `npm ci` installs from the committed lockfile; no new packages are added in this phase, so no Package Legitimacy Gate is triggered. Pin to commit SHAs only if supply-chain posture is later raised. |

No threat reaches the `high` blocking threshold. Plan not blocked.
</threat_model>

<verification>
- Static check (automated above): ci.yml contains npm ci / typecheck / test / both triggers / pinned actions, and contains no SPARKPOST_API_KEY.
- Manual (post-merge, per 01-VALIDATION Manual-Only): push the branch / open a PR and confirm the CI check passes green in the GitHub Actions tab. Not locally assertable.
</verification>

<success_criteria>
- A single-job CI workflow gates push and PR on install → typecheck → test.
- The workflow uses one Node LTS, caches npm, and references no secrets.
</success_criteria>

<output>
Create `.planning/phases/01-test-foundation-ci/01-03-SUMMARY.md` when done.
</output>
