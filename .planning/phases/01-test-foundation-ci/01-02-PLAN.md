---
phase: 01-test-foundation-ci
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - test/handlers.test.ts
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Each of the 8 handlers has a test asserting the exact URL, method, and body it builds against a captured, per-test monkeypatched globalThis.fetch stub restored after each test (D-05)"
    - "asText output is asserted: result.content[0].type === 'text' and the parsed text round-trips the mocked response"
    - "Each parameterized zod schema rejects representative invalid input (e.g. an invalid email)"
    - "test/handlers.test.ts runs under node:test via tsx (D-04), passes, and adds no SPARKPOST_API_KEY to the env"
  artifacts:
    - test/handlers.test.ts
  key_links:
    - "test/handlers.test.ts imports the 8 handlers + 5 zod schemas from ../src/sparkpost.js (created by 01-01)"
    - "A capturing fetch stub records { url, init } in beforeEach and is restored in afterEach"
    - "Assertions use the verbatim URL/method/body shapes from 01-PATTERNS.md"
---

<objective>
Add test/handlers.test.ts covering all 8 shipped tools: for each, assert the request URL/method/body it builds and that asText wraps the response correctly; for each parameterized tool, assert its zod schema rejects bad input. Covers TEST-02.

Purpose: Lock the payload-shaping and validation behavior of every shipped tool so Phase 2's coverage expansion can't silently break an existing tool's request contract.
Output: test/handlers.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-foundation-ci/01-RESEARCH.md
@.planning/phases/01-test-foundation-ci/01-PATTERNS.md
@.planning/phases/01-test-foundation-ci/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol / File | Kind | Notes |
|---|---|---|
| `test/handlers.test.ts` | new test file | TEST-02: 8 tools × (URL/method/body + asText) + zod-rejection for the 5 parameterized tools |

This plan creates no new exported symbols — it consumes the surface 01-01 produced (`src/sparkpost.ts` handlers and schema consts).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write handler payload + zod-validation tests for all 8 tools</name>
  <files>test/handlers.test.ts</files>
  <read_first>
    - .planning/phases/01-test-foundation-ci/01-PATTERNS.md — the "All 8 handler URLs, methods, and body shapes" table (ground truth for every URL/method/body assertion) and the "test/handlers.test.ts" section (capturing-stub pattern + per-handler assertion shape + zod-rejection shape).
    - .planning/phases/01-test-foundation-ci/01-RESEARCH.md — "TEST-02: Handler test pattern (example: create_template)" code example (capturing stub, body JSON parse, asText assertion, direct zod schema test) and the note on testing zod schemas directly rather than through the handler.
    - .planning/phases/01-test-foundation-ci/01-01-SUMMARY.md — confirm the exact exported names of the 8 handlers and the 5 schema consts from src/sparkpost.ts.
    - index.ts (only if a body shape is ambiguous) — the registered handlers are the original source of each URL/method/body.
  </read_first>
  <behavior>
    Capturing stub records `{ url, init }` for each call; restored after each test.

    Per-handler URL/method/body (from PATTERNS.md table — assert these exactly):
    - getAccount → GET, url ends with `/account?include=usage`.
    - listTemplates → GET, url ends with `/templates`.
    - getTemplate({template_id:"abc"}) → GET, url ends with `/templates/abc`; with draft:true → ends with `/templates/abc?draft=true`.
    - createTemplate({id, name, subject, html, from_email}) → POST, url ends with `/templates`; body.id matches, body.content.from.email matches from_email, body.content.subject/html match.
    - updateTemplate({template_id, subject}) → PUT, url ends with `/templates/<id>`; with update_published:true → ends with `?update_published=true`; body.content contains only the defined keys (e.g. subject present, html absent when not passed).
    - sendEmail({to, from_email, subject, html}) → POST, url ends with `/transmissions`; body.recipients[0].address.email === to; inline branch → body.content.from === from_email; template branch (template_id passed) → body.content.template_id set and no inline from.
    - checkSuppression({email:"a@b.com"}) → GET, url ends with `/suppression-list/a%40b.com` (encodeURIComponent).
    - listSendingDomains → GET, url ends with `/sending-domains`.

    asText (assert for at least the parameterized handlers, representative for no-arg ones): `result.content[0].type === "text"` and `JSON.parse(result.content[0].text)` deepEquals the mocked `{ results: ... }` body.

    Zod rejection (the 5 parameterized schemas, representative depth):
    - CreateTemplateSchema / SendEmailSchema: `from_email` (and `to` for send) reject a non-email string.
    - GetTemplateSchema / UpdateTemplateSchema / CheckSuppressionSchema: reject a missing/wrong-typed required field (e.g. CheckSuppressionSchema rejects a non-email `email`; GetTemplateSchema rejects a non-string `template_id`).
  </behavior>
  <action>
    Create test/handlers.test.ts implementing the behaviors above using the capturing-stub pattern from RESEARCH/PATTERNS.

    1. Import `test, beforeEach, afterEach` from `node:test`, `assert` from `node:assert/strict`, `z` from `zod`, and the 8 handlers + 5 schema consts from `../src/sparkpost.js` (`.js` extension, nodenext). Use the exact export names confirmed in 01-01-SUMMARY.md.
    2. Reuse the duck-typed `mockResponse(body, status, statusText = "OK"): Response` helper (same shape as in spRequest.test.ts — `{ ok, status, statusText, text: async () => body } as unknown as Response`). Do NOT use `new Response(...)`.
    3. Capturing stub: `let savedFetch; let capturedRequest: { url: string; init: RequestInit } | null = null;` In `beforeEach`, save fetch, null the capture, and set `globalThis.fetch = async (url, init) => { capturedRequest = { url: String(url), init: init ?? {} }; return mockResponse('{"results":{"id":"tpl-1"}}', 200); }`. In `afterEach`, restore fetch.
    4. For each of the 8 handlers, write a test that calls the handler with valid representative args, then asserts the URL suffix, method, and (for POST/PUT) the parsed body fields per the behavior table. Parse the body via `JSON.parse(capturedRequest?.init.body as string)`. For getTemplate, updateTemplate, and sendEmail, include the second branch (draft / update_published / template-vs-inline) as its own assertion or a second test.
    5. For each handler, assert asText output shape on the returned result (at minimum: `result.content[0].type === "text"` and the parsed text deepEquals the mocked body). Representative depth is acceptable where handlers are near-identical, but every tool gets at least one URL/method assertion.
    6. For each of the 5 parameterized schemas, add a zod-rejection test that wraps the exported schema shape in `z.object(SchemaConst)` and asserts `.parse(badInput)` throws (use `assert.throws`). Test the schema directly — direct handler calls (D-02) bypass the SDK's validation, so schema validation must be asserted against the schema itself.
    Set no SPARKPOST_API_KEY anywhere (fetch is stubbed; no key is needed).
  </action>
  <verify>
    <automated>node --import tsx --test "test/handlers.test.ts" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 8 handlers have at least one URL/method assertion; POST/PUT handlers also assert body fields per the PATTERNS.md table.
    - getTemplate (draft), updateTemplate (update_published + defined-keys-only body), and sendEmail (inline vs template branch) each assert their conditional behavior.
    - asText output shape is asserted (type "text" + round-tripped body).
    - The 5 parameterized schemas each have a passing zod-rejection test.
    - `node --import tsx --test "test/handlers.test.ts"` is green; `npm run typecheck` exits 0.
    - No `new Response(...)` and no SPARKPOST_API_KEY in the file.
  </acceptance_criteria>
  <done>test/handlers.test.ts asserts payload shaping for all 8 tools (including conditional branches) and zod rejection for all 5 parameterized schemas, and runs green alongside typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → SparkPost REST API | Handlers are exercised only against a captured `globalThis.fetch` stub; no request leaves the process. |
| handler args → request body | Tests assert that user-supplied args are shaped into the documented body, and that zod rejects malformed input before a handler would build a request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-IDISC | Information Disclosure | test/handlers.test.ts | low | mitigate | No `SPARKPOST_API_KEY` set in test env; the capturing stub replaces `globalThis.fetch`, so no real auth header is sent. |
| T-01-TAMP | Tampering | capturing fetch stub | low | mitigate | `globalThis.fetch` replaced in `beforeEach` / restored in `afterEach`; no handler call reaches the real SparkPost API. |

No threat reaches the `high` blocking threshold — fully mocked, secret-free. Plan not blocked.
</threat_model>

<verification>
- `node --import tsx --test "test/handlers.test.ts"` → all handler + zod tests pass.
- `npm test` (full suite) → green.
- `npm run typecheck` → exits 0.
</verification>

<success_criteria>
- Every shipped tool's request contract (URL/method/body) and asText wrapping are asserted.
- Every parameterized schema's input validation is asserted via a rejection test.
- The handler suite and typecheck are both green.
</success_criteria>

<output>
Create `.planning/phases/01-test-foundation-ci/01-02-SUMMARY.md` when done.
</output>
