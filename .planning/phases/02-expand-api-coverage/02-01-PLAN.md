---
phase: 02-expand-api-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sparkpost.ts
  - index.ts
  - test/phase2-webhooks-subaccounts.test.ts
autonomous: true
requirements: [COV-01, COV-05]
must_haves:
  truths:
    - "list_webhooks GETs /api/v1/webhooks and returns the page as text"
    - "create_webhook POSTs /api/v1/webhooks with required name/target/events and rejects when any is missing"
    - "delete_webhook DELETEs /api/v1/webhooks/{encoded id} (single id only, no bulk)"
    - "list_subaccounts GETs /api/v1/subaccounts"
    - "create_subaccount POSTs /api/v1/subaccounts with name/key_label/key_grants and rejects key_grants outside the valid enum"
  artifacts:
    - src/sparkpost.ts contains CreateWebhookSchema, DeleteWebhookSchema, CreateSubaccountSchema
    - src/sparkpost.ts contains listWebhooks, createWebhook, deleteWebhook, listSubaccounts, createSubaccount
    - "index.ts registers list_webhooks, create_webhook, delete_webhook, list_subaccounts, create_subaccount"
    - test/phase2-webhooks-subaccounts.test.ts exists and passes
  key_links:
    - "each new server.tool registration in index.ts imports the matching handler+schema from ./src/sparkpost.js"
    - "WEBHOOK_EVENTS enum array feeds create_webhook events field; KEY_GRANTS enum array feeds create_subaccount key_grants"
---

<objective>
Add the webhook tools (list/create/delete) and subaccount tools (list/create) to the existing
SparkPost MCP server, each typed, zod-validated, registered, and tested exactly like the Phase 1
tools. Implements COV-01 (webhooks) and COV-05 (subaccounts).

Purpose: Grow the API surface so an AI client can manage SparkPost webhooks and subaccounts
through the same typed, validated tool layer as the existing 8 tools.
Output: 5 new handlers + 3 new zod schemas appended to src/sparkpost.ts, 5 new server.tool
registrations in index.ts, and a new test file test/phase2-webhooks-subaccounts.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expand-api-coverage/02-RESEARCH.md
@.planning/phases/02-expand-api-coverage/02-PATTERNS.md
</context>

## Artifacts this phase produces

| Kind | Symbol / Path |
|------|---------------|
| const array | `WEBHOOK_EVENTS` (in src/sparkpost.ts) |
| const array | `KEY_GRANTS` (in src/sparkpost.ts) |
| schema | `CreateWebhookSchema` |
| schema | `DeleteWebhookSchema` |
| schema | `CreateSubaccountSchema` |
| handler | `listWebhooks` |
| handler | `createWebhook` |
| handler | `deleteWebhook` |
| handler | `listSubaccounts` |
| handler | `createSubaccount` |
| tool name | `list_webhooks`, `create_webhook`, `delete_webhook`, `list_subaccounts`, `create_subaccount` |
| test file | `test/phase2-webhooks-subaccounts.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test file for webhook + subaccount tools</name>
  <read_first>
    src/sparkpost.ts (the template to copy), test/handlers.test.ts (test boilerplate + zod-rejection pattern), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern A/B/C, shared test boilerplate)
  </read_first>
  <files>test/phase2-webhooks-subaccounts.test.ts</files>
  <behavior>
    - listWebhooks: captured init.url ends with /webhooks, init.method === GET, result.content[0].type === text
    - createWebhook: init.url ends with /webhooks, init.method === POST, parsed body has name/target and body.events is an array
    - createWebhook zod rejection: z.object(CreateWebhookSchema).parse({}) throws (missing name/target/events)
    - deleteWebhook: init.url ends with /webhooks/wh-123, init.method === DELETE
    - listSubaccounts: init.url ends with /subaccounts, init.method === GET
    - createSubaccount: init.url ends with /subaccounts, init.method === POST, body has name/key_label and body.key_grants is an array
    - createSubaccount zod rejection: parse with key_grants:["bogus/grant"] throws (invalid enum member)
  </behavior>
  <action>
    Copy the entire preamble from test/handlers.test.ts verbatim (the mockResponse duck-typed helper, MOCK_BODY/MOCK_DATA, savedFetch/capturedRequest, beforeEach/afterEach). The duck-typed mockResponse is load-bearing — it dodges the Node 24 undici 204 constructor bug; do not replace it with `new Response()`.
    Import the five handlers and three schemas (CreateWebhookSchema, DeleteWebhookSchema, CreateSubaccountSchema) plus WEBHOOK_EVENTS and KEY_GRANTS from "../src/sparkpost.js" (.js extension — ESM).
    Write one test per handler asserting captured url suffix + method (and parsed body fields for the POSTs), mirroring the listTemplates / createTemplate / checkSuppression tests.
    Write two zod-rejection tests using `z.object(SchemaConst).parse(...)` directly (handlers bypass SDK validation, per D-02). Zod v4 messages are lowercase — assert with case-insensitive regex (e.g. /invalid/i or /expected/i), never an exact-case literal.
    For createWebhook, pass a valid events value drawn from WEBHOOK_EVENTS (e.g. "delivery"); for createSubaccount, pass a valid key_grants member from KEY_GRANTS (e.g. "transmissions/view").
    This task runs BEFORE the handlers exist, so the import will not resolve and the suite is RED — that is the expected Wave 0 state. Task 2 makes it GREEN.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -Eq "Cannot find|does not provide an export|fail" && echo "RED as expected (handlers not yet implemented)" || echo "unexpected: suite green before impl"</automated>
  </verify>
  <done>test/phase2-webhooks-subaccounts.test.ts exists with 7 test cases; running it fails only because the imported handlers/schemas do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement webhook + subaccount handlers and schemas</name>
  <read_first>
    src/sparkpost.ts (append after checkSuppression at line 167; copy listTemplates / createTemplate / checkSuppression shapes), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern A no-arg list, Pattern B single path-param, Pattern C POST create; Constant Definition Notes for WEBHOOK_EVENTS and KEY_GRANTS), .planning/phases/02-expand-api-coverage/02-RESEARCH.md (Endpoint Reference Tables COV-01 + COV-05 for exact required fields and enum members)
  </read_first>
  <files>src/sparkpost.ts</files>
  <action>
    APPEND (do not overwrite existing exports) to src/sparkpost.ts. Define two `const` enum arrays first, then schemas, then handlers — matching the existing `export const XxxSchema = {...}` + `export async function xxx(args) { return asText(await spRequest(path, method, body)); }` convention.

    WEBHOOK_EVENTS const: the full webhook event-type list from RESEARCH (bounce, delivery, injection, spam_complaint, out_of_band, policy_rejection, delay, click, open, initial_open, amp_click, amp_open, amp_initial_open, generation_failure, generation_rejection, list_unsubscribe, link_unsubscribe, relay_injection, relay_rejection, relay_delivery, relay_tempfail, relay_permfail, ab_test_completed, ab_test_cancelled, success, error) as `as const`. This is the WEBHOOK set — distinct from the message-events set in plan 02-02 (Pitfall 3). Export it.
    KEY_GRANTS const: smtp/inject, sending_domains/manage, tracking_domains/view, tracking_domains/manage, message_events/view, suppression_lists/manage, transmissions/view, transmissions/modify, webhooks/view, webhooks/modify, as `as const`. Export it.

    listWebhooks (no args): `asText(await spRequest("/webhooks"))`. Mirror listTemplates.
    CreateWebhookSchema + createWebhook: tool create_webhook → POST /webhooks. Required: name (z.string()), target (z.string().url()), events (z.array(z.enum(WEBHOOK_EVENTS)).min(1)). Optional: auth_type (z.enum(["none","basic","oauth2"]).optional().default("none")). Skip auth_credentials/auth_request_details in v1.1 (RESEARCH COV-01 MCP tool design). Body is flat { name, target, events, auth_type } — no nested reshaping.
    DeleteWebhookSchema + deleteWebhook: tool delete_webhook → DELETE /webhooks/{id}. Single id only per D-04 (no bulk, no confirm flag). id is z.string().describe("Webhook UUID"). Path-interpolate with encodeURIComponent(id) defensively (Pattern B). Returns 204 → spRequest yields {} → asText({}) is valid; no special-casing.
    listSubaccounts (no args): `asText(await spRequest("/subaccounts"))`.
    CreateSubaccountSchema + createSubaccount: tool create_subaccount → POST /subaccounts. Required (setup_api_key defaults true): name (z.string()), key_label (z.string()), key_grants (z.array(z.enum(KEY_GRANTS)).min(1)). Body is flat { name, key_label, key_grants }.

    Use z.email()/z.string() top-level forms as in existing code (zod v4). No fenced code — follow the existing file's shapes.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5; npm run typecheck</automated>
  </verify>
  <done>All 7 tests in test/phase2-webhooks-subaccounts.test.ts pass; `npm run typecheck` exits 0; the five handlers and three schemas plus WEBHOOK_EVENTS/KEY_GRANTS are exported from src/sparkpost.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Register webhook + subaccount tools in index.ts</name>
  <read_first>
    index.ts (import block lines 4-18, server.tool block lines 28-35), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (server.tool registration shape)
  </read_first>
  <files>index.ts</files>
  <action>
    Extend the existing import from "./src/sparkpost.js" to add: listWebhooks, createWebhook, CreateWebhookSchema, deleteWebhook, DeleteWebhookSchema, listSubaccounts, createSubaccount, CreateSubaccountSchema.
    APPEND five server.tool(...) calls after the existing list_sending_domains line (line 35), before the transport lines. Pattern: server.tool(snake_name, "description", SchemaOrEmpty, handler).
    - server.tool("list_webhooks", "List all webhooks", {}, listWebhooks)
    - server.tool("create_webhook", "Create a webhook (name, target URL, events)", CreateWebhookSchema, createWebhook)
    - server.tool("delete_webhook", "Delete a single webhook by id", DeleteWebhookSchema, deleteWebhook)
    - server.tool("list_subaccounts", "List all subaccounts", {}, listSubaccounts)
    - server.tool("create_subaccount", "Create a subaccount with an API key (name, key_label, key_grants)", CreateSubaccountSchema, createSubaccount)
    Do not touch the API_KEY guard or transport/connect lines.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c 'server.tool("\(list_webhooks\|create_webhook\|delete_webhook\|list_subaccounts\|create_subaccount\)"' index.ts</automated>
  </verify>
  <done>`npm run typecheck` exits 0; index.ts contains exactly five new server.tool registrations for the webhook + subaccount tools, each wired to its imported handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI client → MCP tool args | Untrusted tool arguments (webhook id, target URL, key_grants, subaccount name) enter here |
| MCP tool → SparkPost REST | spRequest forwards the shaped request; SparkPost is the enforcement point for resource state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | deleteWebhook path (`/webhooks/{id}`) | low | mitigate | encodeURIComponent(id) on the path-interpolated id (Pattern B), preventing path traversal / segment injection |
| T-02-02 | Elevation of Privilege | createSubaccount key_grants | medium | mitigate | z.array(z.enum(KEY_GRANTS)).min(1) restricts grants to the known valid set; AI client must explicitly choose grants, cannot inject arbitrary scopes |
| T-02-03 | Tampering | createWebhook target (SSRF surface) | low | accept | MCP layer makes no outbound call to target; SparkPost enforces ports 80/443 and performs the callback server-side. No mitigation needed at tool layer |
| T-02-04 | Tampering | createWebhook events | low | mitigate | z.array(z.enum(WEBHOOK_EVENTS)).min(1) rejects unknown event names before the request leaves the tool |
| T-02-SC | Tampering | npm installs | low | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: N/A); no install step, no [ASSUMED]/[SUS] packages |

ASVS Level 1, V5 (Input Validation) is the in-scope category; V2/V3/V4/V6 are N/A (RESEARCH Security Domain). No `high` threats — destructive ops are single-target by schema (D-04) and SparkPost owns resource lifecycle.
</threat_model>

<verification>
- `npm test` — full suite green (Phase 1 tests + the 7 new webhook/subaccount tests)
- `npm run typecheck` — exits 0 (strict tsconfig)
- index.ts registers all five new tools; each imports its handler/schema from ./src/sparkpost.js
- Captured request assertions confirm: list_webhooks→GET /webhooks, create_webhook→POST /webhooks, delete_webhook→DELETE /webhooks/{encoded id}, list_subaccounts→GET /subaccounts, create_subaccount→POST /subaccounts
- zod rejects create_webhook missing required fields and create_subaccount invalid key_grants
</verification>

<success_criteria>
- COV-01 satisfied: list_webhooks, create_webhook, delete_webhook exist, validated, tested, registered
- COV-05 satisfied: list_subaccounts, create_subaccount exist, validated, tested, registered
- Every new tool has a test asserting URL/method/body and (for the create_* tools) a zod-rejection
- `npm test` and `npm run typecheck` both pass
</success_criteria>

<output>
Create `.planning/phases/02-expand-api-coverage/02-01-SUMMARY.md` when done
</output>
