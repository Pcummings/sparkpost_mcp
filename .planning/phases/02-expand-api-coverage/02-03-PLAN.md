---
phase: 02-expand-api-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sparkpost.ts
  - index.ts
  - test/phase2-lists-suppression.test.ts
autonomous: true
requirements: [COV-03, COV-04]
must_haves:
  truths:
    - "list_recipient_lists GETs /api/v1/recipient-lists"
    - "get_recipient_list GETs /api/v1/recipient-lists/{encoded id} and appends ?show_recipients=true when requested"
    - "create_recipient_list POSTs /api/v1/recipient-lists with only recipients[] required, reshaping each {email,name} to {address:{email,name}}, and rejects an empty recipients array"
    - "add_suppression PUTs /api/v1/suppression-list/{encoded email} with {type, description}, type a required enum, and rejects an invalid type"
    - "remove_suppression DELETEs /api/v1/suppression-list/{encoded email} (single email only, no bulk)"
  artifacts:
    - src/sparkpost.ts contains GetRecipientListSchema, CreateRecipientListSchema, AddSuppressionSchema, RemoveSuppressionSchema
    - src/sparkpost.ts contains listRecipientLists, getRecipientList, createRecipientList, addSuppression, removeSuppression
    - "index.ts registers list_recipient_lists, get_recipient_list, create_recipient_list, add_suppression, remove_suppression"
    - test/phase2-lists-suppression.test.ts exists and passes
  key_links:
    - "create_recipient_list maps the flat recipients input to SparkPost's {address:{email,name}} shape inside the handler"
    - "add_suppression / remove_suppression / get_recipient_list path-interpolate via encodeURIComponent (email and id)"
---

<objective>
Add recipient-list tools (list/get/create) and suppression-management tools (add/remove) to the
SparkPost MCP server, each typed, zod-validated, registered, and tested like the Phase 1 tools.
Implements COV-03 (recipient lists) and COV-04 (suppression add/remove — the existing
check_suppression already covers read).

Purpose: Let an AI client manage recipient lists and add/remove suppression entries through the
same typed tool layer, with single-target destructive ops (D-04) and the upsert suppression shape
(D-05).
Output: 5 new handlers + 4 new zod schemas appended to src/sparkpost.ts, 5 new server.tool
registrations in index.ts, and a new test file test/phase2-lists-suppression.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expand-api-coverage/02-RESEARCH.md
@.planning/phases/02-expand-api-coverage/02-PATTERNS.md
</context>

## Artifacts this phase produces

| Kind | Symbol / Path |
|------|---------------|
| schema | `GetRecipientListSchema` |
| schema | `CreateRecipientListSchema` |
| schema | `AddSuppressionSchema` |
| schema | `RemoveSuppressionSchema` |
| handler | `listRecipientLists` |
| handler | `getRecipientList` |
| handler | `createRecipientList` |
| handler | `addSuppression` |
| handler | `removeSuppression` |
| tool name | `list_recipient_lists`, `get_recipient_list`, `create_recipient_list`, `add_suppression`, `remove_suppression` |
| test file | `test/phase2-lists-suppression.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test file for recipient-list + suppression tools</name>
  <read_first>
    src/sparkpost.ts (checkSuppression for the single path-param + encodeURIComponent pattern; createTemplate for body-building; getTemplate for the conditional ?suffix), test/handlers.test.ts (test boilerplate + zod-rejection pattern), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern A, B, C, D), .planning/phases/02-expand-api-coverage/02-RESEARCH.md (Endpoint Reference Tables COV-03 + COV-04)
  </read_first>
  <files>test/phase2-lists-suppression.test.ts</files>
  <behavior>
    - listRecipientLists (no args): init.url ends with /recipient-lists, init.method === GET
    - getRecipientList: init.url ends with /recipient-lists/list-1, init.method === GET
    - getRecipientList with show_recipients true: init.url ends with /recipient-lists/list-1?show_recipients=true
    - createRecipientList: init.url ends with /recipient-lists, init.method === POST; parsed body.recipients[0].address.email equals the input email (reshape verified); body has no top-level id when omitted
    - createRecipientList zod rejection: parse with recipients:[] throws (empty array violates .min(1))
    - addSuppression: init.url ends with /suppression-list/<encoded email>, init.method === PUT; parsed body.type equals the supplied enum, body.description present when supplied
    - addSuppression zod rejection: parse with type:"bogus" throws (invalid enum)
    - removeSuppression: init.url ends with /suppression-list/<encoded email>, init.method === DELETE
  </behavior>
  <action>
    Copy the test preamble from test/handlers.test.ts verbatim (duck-typed mockResponse — load-bearing for the Node 24 undici 204 bug; MOCK_BODY/MOCK_DATA; savedFetch/capturedRequest; beforeEach/afterEach).
    Import listRecipientLists, getRecipientList, createRecipientList, addSuppression, removeSuppression and GetRecipientListSchema, CreateRecipientListSchema, AddSuppressionSchema, RemoveSuppressionSchema from "../src/sparkpost.js".
    For the encoded-email assertions, build the expected suffix with encodeURIComponent(email) exactly as the existing checkSuppression test does (use an email containing @, e.g. "a@b.com").
    For createRecipientList, pass recipients as an array of flat { email, name? } and assert the handler reshaped element 0 to { address: { email, name } } in the captured POST body.
    Write zod-rejection tests with `z.object(SchemaConst).parse(...)` directly. Zod v4 messages are lowercase — case-insensitive regex (/invalid|too_small|at least|expected/i), never an exact-case literal.
    The handlers do not exist yet, so imports fail and the suite is RED — expected Wave 0 state.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -Eq "Cannot find|does not provide an export|fail" && echo "RED as expected" || echo "unexpected: green before impl"</automated>
  </verify>
  <done>test/phase2-lists-suppression.test.ts exists covering all five tools (URL/method/body, show_recipients suffix, recipient reshape, encoded-email paths) plus the empty-recipients and invalid-type zod rejections; it fails only because the handlers/schemas are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recipient-list + suppression handlers and schemas</name>
  <read_first>
    src/sparkpost.ts (append after the last existing export; copy checkSuppression for path encoding, getTemplate for conditional ?suffix, createTemplate for body-building), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern A, B, C, D; encodeURIComponent shared pattern), .planning/phases/02-expand-api-coverage/02-RESEARCH.md (Endpoint Reference Tables COV-03 recipient object shape + COV-04 suppression type enum; Pitfall 4 type field, Pitfall 6 id auto-generation)
  </read_first>
  <files>src/sparkpost.ts</files>
  <action>
    APPEND to src/sparkpost.ts (do not overwrite existing exports). Follow the existing `export const XxxSchema = {...}` + handler convention.

    listRecipientLists (no args): `asText(await spRequest("/recipient-lists"))`. Mirror listTemplates.

    GetRecipientListSchema + getRecipientList: tool get_recipient_list → GET /recipient-lists/{id}. id (z.string()); show_recipients (z.boolean().optional().default(false)). Path: `/recipient-lists/${encodeURIComponent(id)}` + (show_recipients ? "?show_recipients=true" : "") — same conditional-suffix shape as getTemplate.

    CreateRecipientListSchema + createRecipientList: tool create_recipient_list → POST /recipient-lists. ONLY recipients is required (RESEARCH correction + Pitfall 6): recipients (z.array(z.object({ email: z.email(), name: z.string().optional() })).min(1)). Optional: id (z.string().optional()), name (z.string().optional()), description (z.string().optional()). Handler reshapes each recipient to { address: { email, name } } and builds body { recipients: mapped, ...(id && {id}), ...(name && {name}), ...(description && {description}) } — omit id/name/description when not supplied so SparkPost auto-generates id.

    AddSuppressionSchema + addSuppression: tool add_suppression → PUT /suppression-list/{email} (upsert, D-05). email (z.email()); type (z.enum(["transactional","non_transactional"]).default("non_transactional")) — single enum, NOT booleans (Pitfall 4); description (z.string().optional()). Path: `/suppression-list/${encodeURIComponent(email)}`, method "PUT", body { type, description }. Pattern D.

    RemoveSuppressionSchema + removeSuppression: tool remove_suppression → DELETE /suppression-list/{email}. Single email only per D-04 (no bulk). email (z.email()). Path: `/suppression-list/${encodeURIComponent(email)}`, method "DELETE". Returns 204 → spRequest {} → asText valid; no special-casing. Mirror checkSuppression with method DELETE.

    Use z.email()/z.string() top-level forms as in existing code (zod v4).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5; npm run typecheck</automated>
  </verify>
  <done>All tests in test/phase2-lists-suppression.test.ts pass; `npm run typecheck` exits 0; the five handlers and four schemas are exported from src/sparkpost.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Register recipient-list + suppression tools in index.ts</name>
  <read_first>
    index.ts (import block + server.tool block), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (server.tool registration shape)
  </read_first>
  <files>index.ts</files>
  <action>
    Extend the import from "./src/sparkpost.js" to add: listRecipientLists, getRecipientList, GetRecipientListSchema, createRecipientList, CreateRecipientListSchema, addSuppression, AddSuppressionSchema, removeSuppression, RemoveSuppressionSchema.
    APPEND five server.tool(...) calls in the registration block (before the transport lines):
    - server.tool("list_recipient_lists", "List all recipient lists", {}, listRecipientLists)
    - server.tool("get_recipient_list", "Get a recipient list by id (optionally with recipients)", GetRecipientListSchema, getRecipientList)
    - server.tool("create_recipient_list", "Create a recipient list (recipients required; id/name auto-generated if omitted)", CreateRecipientListSchema, createRecipientList)
    - server.tool("add_suppression", "Add or update a suppression entry for one email", AddSuppressionSchema, addSuppression)
    - server.tool("remove_suppression", "Remove the suppression entry for one email", RemoveSuppressionSchema, removeSuppression)
    Do not touch the API_KEY guard or transport/connect lines. Append distinct registrations — do not reorder or overwrite sibling registrations from plans 02-01/02-02.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c 'server.tool("\(list_recipient_lists\|get_recipient_list\|create_recipient_list\|add_suppression\|remove_suppression\)"' index.ts</automated>
  </verify>
  <done>`npm run typecheck` exits 0; index.ts contains exactly five new server.tool registrations wired to their imported handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI client → MCP tool args | Untrusted args (recipient-list id, recipient emails, suppression email + type) enter here |
| MCP tool → SparkPost REST | spRequest forwards the shaped request; SparkPost owns list/suppression state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | addSuppression / removeSuppression path (`/suppression-list/{email}`) | low | mitigate | encodeURIComponent(email) on the path-interpolated email (emails contain @/+), preventing path traversal — same control as existing checkSuppression |
| T-02-10 | Tampering | getRecipientList path (`/recipient-lists/{id}`) | low | mitigate | encodeURIComponent(id) on the path-interpolated id |
| T-02-11 | Tampering | addSuppression type field | low | mitigate | z.enum(["transactional","non_transactional"]) rejects deprecated boolean style / invalid values (Pitfall 4) |
| T-02-12 | Tampering | createRecipientList recipients | low | mitigate | z.array(z.object({email: z.email()})).min(1) validates each recipient email and forbids an empty list before the request leaves the tool |
| T-02-SC | Tampering | npm installs | low | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: N/A); no install step |

ASVS Level 1, V5 (Input Validation) in scope; V2/V3/V4/V6 N/A (RESEARCH Security Domain). No `high` threats — destructive remove is single-email by schema (D-04); SparkPost owns suppression/list lifecycle.
</threat_model>

<verification>
- `npm test` — full suite green (Phase 1 + the new recipient-list/suppression tests)
- `npm run typecheck` — exits 0
- create_recipient_list reshapes flat {email,name} → {address:{email,name}}, requires non-empty recipients, omits id/name when absent
- add_suppression PUTs {type, description} to the encoded-email path with type as a required enum; remove_suppression DELETEs the encoded-email path
- get_recipient_list appends ?show_recipients=true only when requested
- index.ts registers all five new tools wired to their handlers
</verification>

<success_criteria>
- COV-03 satisfied: list_recipient_lists, get_recipient_list, create_recipient_list exist, validated, tested, registered
- COV-04 satisfied: add_suppression, remove_suppression exist, validated, tested, registered
- Single-target destructive op (remove_suppression takes one email, D-04); suppression upsert shape (D-05)
- `npm test` and `npm run typecheck` both pass
</success_criteria>

<output>
Create `.planning/phases/02-expand-api-coverage/02-03-SUMMARY.md` when done
</output>
