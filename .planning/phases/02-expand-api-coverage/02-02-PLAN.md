---
phase: 02-expand-api-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sparkpost.ts
  - index.ts
  - test/phase2-events-metrics.test.ts
autonomous: true
requirements: [COV-02]
must_haves:
  truths:
    - "search_message_events GETs /api/v1/events/message with a query string built from typed filters and surfaces the response links/cursor as text"
    - "search_message_events joins the events array comma-delimited and omits unset params; first call sends no cursor"
    - "search_message_events rejects unknown event types via zod enum"
    - "get_deliverability_metrics requires from AND metrics, joins metrics comma-delimited, and rejects when either is missing"
    - "get_deliverability_metrics maps group_by to a URL path suffix (/metrics/deliverability/{group_by}), never a ?group_by= query param"
  artifacts:
    - src/sparkpost.ts contains SearchMessageEventsSchema and GetDeliverabilityMetricsSchema
    - src/sparkpost.ts contains searchMessageEvents and getDeliverabilityMetrics
    - src/sparkpost.ts contains MESSAGE_EVENT_TYPES enum array
    - "index.ts registers search_message_events and get_deliverability_metrics"
    - test/phase2-events-metrics.test.ts exists and passes
  key_links:
    - "search_message_events builds its query with URLSearchParams; events.join(',') feeds the events param"
    - "get_deliverability_metrics group_by feeds the path suffix while from/metrics/to feed URLSearchParams"
---

<objective>
Add the analytics tools — search_message_events (cursor-paginated event search) and
get_deliverability_metrics (aggregate metrics with group-by dimension) — to the SparkPost MCP
server, each typed, zod-validated, registered, and tested like the Phase 1 tools. Implements
COV-02.

Purpose: Let an AI client query message-event history and deliverability metrics through typed,
curated filters (D-02) returning one page (D-01) with dates as ISO-8601 strings (D-03).
Output: 2 new handlers + 2 new zod schemas + the MESSAGE_EVENT_TYPES enum appended to
src/sparkpost.ts, 2 new server.tool registrations in index.ts, and a new test file
test/phase2-events-metrics.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expand-api-coverage/02-RESEARCH.md
@.planning/phases/02-expand-api-coverage/02-PATTERNS.md
</context>

## Artifacts this phase produces

| Kind | Symbol / Path |
|------|---------------|
| const array | `MESSAGE_EVENT_TYPES` (in src/sparkpost.ts) |
| schema | `SearchMessageEventsSchema` |
| schema | `GetDeliverabilityMetricsSchema` |
| handler | `searchMessageEvents` |
| handler | `getDeliverabilityMetrics` |
| tool name | `search_message_events`, `get_deliverability_metrics` |
| test file | `test/phase2-events-metrics.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing test file for events + metrics tools</name>
  <read_first>
    src/sparkpost.ts (the template + spRequest return shape), test/handlers.test.ts (test boilerplate + zod-rejection pattern), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern E query-string GET, Pattern F query-string + path suffix), .planning/phases/02-expand-api-coverage/02-RESEARCH.md (Patterns 4 and 5 for exact param handling)
  </read_first>
  <files>test/phase2-events-metrics.test.ts</files>
  <behavior>
    - searchMessageEvents (no args): init.url ends with /events/message (no query string), init.method === GET
    - searchMessageEvents with filters: init.url contains events=delivery%2Cbounce (comma-encoded) and per_page=2; cursor absent when not supplied
    - searchMessageEvents surfaces response: parsed result text deep-equals the mock (the mock body carries a links key so the test proves links is returned as-is)
    - searchMessageEvents zod rejection: parse with events:["not_a_real_event"] throws
    - getDeliverabilityMetrics: init.url path contains /metrics/deliverability/domain (group_by as PATH suffix) and query contains from=... and metrics=count_delivered%2Ccount_bounce; assert url does NOT contain group_by= (negative assert it is not a query param)
    - getDeliverabilityMetrics no group_by: url path is /metrics/deliverability (no suffix) with from+metrics query
    - getDeliverabilityMetrics zod rejection: parse with {} throws (missing required from AND metrics); a second parse with from set but metrics omitted also throws
  </behavior>
  <action>
    Copy the test preamble from test/handlers.test.ts verbatim (duck-typed mockResponse — load-bearing for the Node 24 undici 204 bug; MOCK_BODY/MOCK_DATA; savedFetch/capturedRequest; beforeEach/afterEach).
    Add a second mock body constant that includes a `links` object (e.g. an object with a results array and a links.next URL string) so the "surfaces links/cursor" test can assert the cursor info is returned unchanged via asText. Keep the default beforeEach mock returning the base MOCK_BODY; override globalThis.fetch inside the specific test that needs the links body, or capture from the default — choose the simplest that lets you assert links round-trips.
    Import searchMessageEvents, getDeliverabilityMetrics, SearchMessageEventsSchema, GetDeliverabilityMetricsSchema, and MESSAGE_EVENT_TYPES from "../src/sparkpost.js".
    For the query-string assertions, assert on substrings of capturedRequest.url (URLSearchParams percent-encodes the comma in joined arrays as %2C). For group_by, assert the path segment /metrics/deliverability/domain is present AND that the string "group_by=" is NOT present (Pitfall 2 — group_by must be a path suffix).
    Write the zod-rejection tests with `z.object(SchemaConst).parse(...)` directly. Zod v4 messages are lowercase — use case-insensitive regex (/invalid|required|expected/i), never an exact-case literal.
    Use a valid event type from MESSAGE_EVENT_TYPES (e.g. "delivery") for the happy path.
    The handlers do not exist yet, so the import fails and the suite is RED — expected Wave 0 state.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -Eq "Cannot find|does not provide an export|fail" && echo "RED as expected" || echo "unexpected: green before impl"</automated>
  </verify>
  <done>test/phase2-events-metrics.test.ts exists covering both tools (happy path, query/path construction, links round-trip, and zod rejections); it fails only because the handlers/schemas are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement searchMessageEvents + getDeliverabilityMetrics</name>
  <read_first>
    src/sparkpost.ts (append after the last existing export; reuse spRequest/asText), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (Pattern E, Pattern F, Constant Definition Notes for MESSAGE_EVENT_TYPES and GROUP_BY), .planning/phases/02-expand-api-coverage/02-RESEARCH.md (Endpoint Reference Tables COV-02 Message Events + Deliverability Metrics; Patterns 4 and 5; Pitfalls 1, 2, 3)
  </read_first>
  <files>src/sparkpost.ts</files>
  <action>
    APPEND to src/sparkpost.ts (do not overwrite). Use stdlib URLSearchParams to build query strings — one builder per handler, no shared util (RESEARCH anti-patterns).

    MESSAGE_EVENT_TYPES const (`as const`, exported): the message-events SUBSET from RESEARCH — bounce, delivery, injection, spam_complaint, out_of_band, policy_rejection, delay, click, open, initial_open, amp_click, amp_open, amp_initial_open, generation_failure, generation_rejection, list_unsubscribe, link_unsubscribe. This is DISTINCT from WEBHOOK_EVENTS in plan 02-01 (no relay/ab-test/success/error) — Pitfall 3.

    SearchMessageEventsSchema + searchMessageEvents: tool search_message_events → GET /events/message. All params optional (D-01/D-02): from (z.string() ISO-8601), to (z.string()), events (z.array(z.enum(MESSAGE_EVENT_TYPES)).optional()), recipients (z.string() comma-delimited emails), recipient_domains (z.string()), per_page (z.number().int().min(1).max(10000).optional()), cursor (z.string().optional()). Handler: build URLSearchParams, guard every set (omit undefined); events → params.set("events", events.join(",")); per_page → String(per_page); DO NOT send cursor on first call (only set when provided — never hard-code "initial"). Path: `/events/message` + (qs ? "?"+qs : ""). Return asText of the raw response so links/next cursor surface unchanged (D-01).

    GetDeliverabilityMetricsSchema + getDeliverabilityMetrics: tool get_deliverability_metrics → GET /metrics/deliverability[/{group_by}]. REQUIRED: from (z.string() ISO-8601), metrics (z.array(z.string()).min(1) — per RESEARCH planner note, not an enum; document valid metric names in .describe()). Optional: to (z.string()), group_by (z.enum(["domain","sending-ip","ip-pool","sending-domain","subaccount","campaign","template"]).optional()), limit (z.number().int().min(1).max(10000).optional()), timezone (z.string().optional()). Handler: params.set("from", from) and params.set("metrics", metrics.join(",")) UNCONDITIONALLY (both required); guard to/limit/timezone; suffix = group_by ? `/${group_by}` : "" — group_by is a PATH segment, never a query param (Pitfall 2). Path: `/metrics/deliverability${suffix}?${params.toString()}`.

    Follow the existing zod v4 top-level forms. No fenced code in implementation beyond the file's existing style.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5; npm run typecheck</automated>
  </verify>
  <done>All tests in test/phase2-events-metrics.test.ts pass; `npm run typecheck` exits 0; searchMessageEvents, getDeliverabilityMetrics, both schemas, and MESSAGE_EVENT_TYPES are exported from src/sparkpost.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Register events + metrics tools in index.ts</name>
  <read_first>
    index.ts (import block + server.tool block), .planning/phases/02-expand-api-coverage/02-PATTERNS.md (server.tool registration shape)
  </read_first>
  <files>index.ts</files>
  <action>
    Extend the import from "./src/sparkpost.js" to add: searchMessageEvents, SearchMessageEventsSchema, getDeliverabilityMetrics, GetDeliverabilityMetricsSchema.
    APPEND two server.tool(...) calls in the registration block (before the transport lines):
    - server.tool("search_message_events", "Search message events (one page; cursor + per_page for paging)", SearchMessageEventsSchema, searchMessageEvents)
    - server.tool("get_deliverability_metrics", "Get deliverability metrics (from + metrics required; group_by selects a breakdown dimension)", GetDeliverabilityMetricsSchema, getDeliverabilityMetrics)
    Do not touch the API_KEY guard or transport/connect lines. This plan and plans 02-01/02-03 each append distinct registrations — append, do not reorder or overwrite sibling registrations.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c 'server.tool("\(search_message_events\|get_deliverability_metrics\)"' index.ts</automated>
  </verify>
  <done>`npm run typecheck` exits 0; index.ts contains the two new server.tool registrations wired to their imported handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI client → MCP tool args | Untrusted filter args (event types, metric names, date strings, group_by) enter here |
| MCP tool → SparkPost REST | spRequest forwards the query; SparkPost validates filter/date/metric semantics server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | searchMessageEvents events filter | low | mitigate | z.array(z.enum(MESSAGE_EVENT_TYPES)) rejects unknown/injected event names; URLSearchParams encodes all values |
| T-02-06 | Tampering | getDeliverabilityMetrics metrics list | low | mitigate | z.array(z.string()).min(1) ensures non-empty; SparkPost validates metric names server-side and returns 400 on unknown (RESEARCH Security Domain) |
| T-02-07 | Tampering | getDeliverabilityMetrics group_by → path | low | mitigate | group_by is constrained by zod enum and mapped to a fixed path-suffix set, so no arbitrary path segment can be injected |
| T-02-08 | Information Disclosure | one-page result bound | low | accept | D-01 returns a single page with cursor/links; the AI client drives paging. No server-side aggregation, bounding token/data exposure per request |
| T-02-SC | Tampering | npm installs | low | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: N/A); no install step |

ASVS Level 1, V5 (Input Validation) in scope; V2/V3/V4/V6 N/A (RESEARCH Security Domain). No `high` threats — query construction is stdlib URLSearchParams over zod-validated typed fields.
</threat_model>

<verification>
- `npm test` — full suite green (Phase 1 + the new events/metrics tests)
- `npm run typecheck` — exits 0
- search_message_events builds the correct query string, joins events comma-delimited, omits unset params, surfaces links/cursor unchanged
- get_deliverability_metrics requires from AND metrics, maps group_by to a path suffix (asserted NOT a query param), rejects when from or metrics is missing
- index.ts registers both new tools wired to their handlers
</verification>

<success_criteria>
- COV-02 satisfied: search_message_events and get_deliverability_metrics exist, validated, tested, registered
- Cursor pagination params exposed; first call sends no cursor; response links round-trip (D-01)
- group_by encoded as path suffix, not query param (Pitfall 2)
- Dates accepted as ISO-8601 strings (D-03)
- `npm test` and `npm run typecheck` both pass
</success_criteria>

<output>
Create `.planning/phases/02-expand-api-coverage/02-02-SUMMARY.md` when done
</output>
