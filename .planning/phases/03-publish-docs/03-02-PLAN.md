---
phase: 03-publish-docs
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "README's Tools table lists all 20 tools by their exact registered names"
    - "README shows the published-package MCP client config using the npx -y sparkpost-mcp bin form, plus a local-dev form"
    - "README has worked usage examples for the key tools (send_email, create_template, search_message_events)"
  artifacts:
    - "README.md extended in place (Tools table expanded to 20, MCP config block updated, Usage examples section added)"
  key_links:
    - "README tool names <-> server.tool registrations in index.ts (names/descriptions copied verbatim, not invented)"
    - "README published config (npx -y sparkpost-mcp) <-> package.json bin from 03-01 (config invokes the bin 03-01 made resolvable)"
---

<objective>
Extend `README.md` so the published package is self-documenting: a categorized table of all 20 tools (names/descriptions verbatim from `index.ts`), worked usage examples for the key tools, and an MCP client config block for the published package (`npx -y sparkpost-mcp`) alongside the existing local-dev form.

Purpose: PKG-01 — a user who finds the package on npm can see every tool it exposes and copy a working MCP client config.
Output: Updated `README.md` (extended, not rewritten).

Per D-04 interpretation lock (do NOT treat as a coverage gap): "documents every tool with a usage example" = the table covers all 20 tools AND representative worked examples exist for the key tools. Per-tool examples are explicitly NOT required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-publish-docs/03-CONTEXT.md
@.planning/phases/03-publish-docs/03-PATTERNS.md
@index.ts
</context>

<artifacts_this_phase_produces>
This plan (03-02) modifies `README.md` in place — the drift/source-grounding pass must treat these README additions as NEW, not pre-existing:

- **Expanded section:** Tools table — grows from 8 rows to all 20 tools.
- **Updated section:** MCP client config — split into a published-package form (`npx -y sparkpost-mcp`) and a local-dev form.
- **New section:** Usage examples — worked prompts for `send_email`, `create_template`, `search_message_events`.

(03-01 separately produces the LICENSE and package.json packaging fields. The published config in this README depends on the bin made resolvable by 03-01.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update the MCP client config block (published + local-dev forms)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current MCP client config block is lines 14-26 — a single local-dev block using the absolute-path tsx form)
    - .planning/phases/03-publish-docs/03-PATTERNS.md (Section 3 shows the exact replacement: published form first, local-dev second)
    - .planning/phases/03-publish-docs/03-RESEARCH.md (MCP Client Config Patterns section — the published form uses args ["-y", "sparkpost-mcp"]; the -y flag auto-confirms npx's first-run install prompt so Claude Desktop does not hang)
    - .planning/phases/03-publish-docs/03-CONTEXT.md (D-04: update config to the published-package form, keep a local-dev form)
  </read_first>
  <action>
    Replace the single `## MCP client config` JSON block (currently the local-dev absolute-path form) with TWO labelled sub-sections under the same `## MCP client config` heading, in this order:
    1. A "Published package (recommended)" form: `mcpServers.sparkpost` with `"command": "npx"`, `"args": ["-y", "sparkpost-mcp"]`, and `"env": { "SPARKPOST_API_KEY": "your-key" }`. The `-y` flag is required — it auto-confirms npx's install prompt so the client does not hang on first run (per RESEARCH MCP Client Config Patterns).
    2. A "Local dev" form: keep the existing shape — `"command": "npx"`, `"args": ["tsx", "/absolute/path/to/index.ts"]`, same `env`.
    Both blocks keep the `SPARKPOST_API_KEY` env var. Do NOT remove the local-dev form (D-04 says keep it). Use bold labels (e.g. Published package recommended and Local dev) above each JSON fence to match the existing `##`-heading doc style.
  </action>
  <verify>
    <automated>grep -q 'sparkpost-mcp' README.md && grep -q '"-y"' README.md && grep -q 'tsx' README.md && grep -q 'absolute/path/to/index.ts' README.md && test $(grep -c 'SPARKPOST_API_KEY' README.md) -ge 2 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README contains a published-package config block whose args array contains both -y and sparkpost-mcp.
    - README still contains a local-dev config block referencing tsx and /absolute/path/to/index.ts.
    - SPARKPOST_API_KEY appears in both config blocks (grep count >= 2).
    - Both blocks sit under the `## MCP client config` heading.
  </acceptance_criteria>
  <done>MCP client config shows the published npx -y sparkpost-mcp form (recommended) plus the retained local-dev form, both with SPARKPOST_API_KEY.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the Tools table to all 20 tools and add Usage examples</name>
  <files>README.md</files>
  <read_first>
    - README.md (current Tools table is lines 28-40 with 8 rows; columns are Tool and Description; Dev block follows at lines 42-46)
    - index.ts lines 49-69 (THE source of truth — the 20 server.tool(name, description, ...) registrations; copy names and descriptions verbatim, do not invent or paraphrase)
    - .planning/phases/03-publish-docs/03-PATTERNS.md (Section 4 lists all 20 rows with verbatim descriptions already extracted; Section 5 gives the three usage-example prompts)
    - .planning/phases/03-publish-docs/03-CONTEXT.md (D-04: single categorized table of all 20; worked examples for KEY tools only — send_email, create_template, search_message_events)
  </read_first>
  <action>
    Two edits to README.md, keeping the existing Tool/Description two-column markdown layout and `##` heading style:
    1. EXPAND the `## Tools` table from 8 rows to all 20 rows. Copy each tool's name and description VERBATIM from the server.tool(...) registrations in `index.ts` lines 49-69. The 20 names in registration order: get_account, list_templates, get_template, create_template, update_template, send_email, check_suppression, list_sending_domains, list_webhooks, create_webhook, delete_webhook, list_subaccounts, create_subaccount, search_message_events, get_deliverability_metrics, list_recipient_lists, get_recipient_list, create_recipient_list, add_suppression, remove_suppression. Do NOT invent names or rewrite descriptions — use the exact strings from index.ts (PATTERNS.md Section 4 already has them extracted verbatim).
    2. ADD a new `## Usage examples` section between the Tools table and the existing `## Dev` block. Per D-04, include worked examples for the KEY tools only: send_email, create_template, search_message_events. Format each as a natural-language prompt a user types to Claude with realistic parameter values (e.g. a send to alice@example.com from noreply@yourdomain.com with subject and HTML body; a template create with id/name/subject/html/from; a message-events search for bounces in a recent window). Do NOT add one example per tool — that is explicitly out of scope (D-04 interpretation lock).
    Keep the existing title, Setup, and Dev sections unchanged.
  </action>
  <verify>
    <automated>missing=""; for t in get_account list_templates get_template create_template update_template send_email check_suppression list_sending_domains list_webhooks create_webhook delete_webhook list_subaccounts create_subaccount search_message_events get_deliverability_metrics list_recipient_lists get_recipient_list create_recipient_list add_suppression remove_suppression; do grep -q "$t" README.md || missing="$missing $t"; done; grep -qi 'usage examples' README.md || missing="$missing USAGE_SECTION"; test -z "$missing" && echo PASS || echo "FAIL missing:$missing"</automated>
  </verify>
  <acceptance_criteria>
    - All 20 tool names appear in README.md (one grep per name; none missing).
    - A `## Usage examples` heading exists in README.md.
    - Worked example prompts are present for send_email, create_template, and search_message_events.
    - The Tools table retains the `| Tool | Description |` two-column layout.
    - Existing title, Setup, and Dev sections are unchanged.
  </acceptance_criteria>
  <done>Tools table covers all 20 tools with verbatim names/descriptions; a Usage examples section documents the three key tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader -> their MCP client config | A user copies the documented `npx -y sparkpost-mcp` config and a `SPARKPOST_API_KEY` placeholder into their own client config file. |

No runtime trust boundaries are added or changed by this plan — it edits documentation only (README.md). No auth, input handling, sessions, access control, or crypto code is touched. (03-RESEARCH.md `## Security Domain`: Phase 3 adds no V2–V6 ASVS-relevant code.)

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-DOC | Information Disclosure | README config examples | low | mitigate | The documented config uses the placeholder `"your-key"` for `SPARKPOST_API_KEY`, never a real credential, so copy-pasting the README cannot leak a live key. Task 1 asserts the env var is present as a named placeholder, not a literal secret. |

No high-severity threats apply (`security_block_on: high`). This is a docs-only plan; per 03-RESEARCH.md `## Security Domain`, Phase 3 adds no authentication, session, access-control, input-validation, or cryptography code, and README content carries no security risk beyond using placeholder credentials (which it does).
</threat_model>

<verification>
- All 20 `server.tool` names from `index.ts` appear in the README Tools table.
- README shows the published `npx -y sparkpost-mcp` config and the local-dev `tsx` config, each with `SPARKPOST_API_KEY`.
- A `## Usage examples` section documents `send_email`, `create_template`, and `search_message_events`.
- Existing title, Setup, and Dev sections are intact (README extended, not rewritten).
</verification>

<success_criteria>
- ROADMAP success criterion 1 met (per D-04 interpretation): README documents every tool (all 20 in the table) with representative usage examples (key tools) and shows MCP client config.
- PKG-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-publish-docs/03-02-SUMMARY.md` when done.
</output>
