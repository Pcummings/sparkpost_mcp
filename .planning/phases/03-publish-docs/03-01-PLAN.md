---
phase: 03-publish-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - LICENSE
  - tsconfig.json.bak
autonomous: true
requirements: [PKG-02]

must_haves:
  truths:
    - "`npm publish --dry-run` exits 0 (prepublishOnly tsc --noEmit passes) and emits a clean minimal tarball"
    - "The published tarball excludes test/, .planning/, .github/, and tsconfig.json.bak"
    - "An installed copy can run the bin because tsx resolves as a runtime dependency"
    - "package.json license field and the LICENSE file both say MIT"
  artifacts:
    - "LICENSE (repo root, MIT text, copyright Peter Cummings)"
    - "package.json with files allowlist, license=MIT, author, prepublishOnly, tsx in dependencies"
  key_links:
    - "files allowlist <-> npm pack tarball contents (allowlist drives what ships)"
    - "tsx in dependencies <-> bin shebang in index.ts (bin needs tsx resolvable post-install)"
    - "prepublishOnly tsc --noEmit <-> npm publish --dry-run (gate only fires on publish, not on pack)"
---

<objective>
Make the existing 20-tool SparkPost MCP server publish-ready on npm without changing any tool behavior. Edit `package.json` packaging metadata, add an MIT `LICENSE`, and remove stray cruft so `npm publish --dry-run` produces a clean, minimal tarball.

Purpose: PKG-02 — the package must be installable and the published artifact must contain only the source + docs needed to run, not tests/planning/config backups.
Output: Updated `package.json`, new `LICENSE`, `tsconfig.json.bak` deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-publish-docs/03-CONTEXT.md
@.planning/phases/03-publish-docs/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (03-01) creates / modifies the following — the drift/source-grounding pass must treat these as NEW, not pre-existing:

- **New file:** `LICENSE` (repo root, MIT text)
- **package.json new fields:** `files` (allowlist array), `prepublishOnly` script, `engines.node`, populated `keywords`
- **package.json changed fields:** `license` (ISC to MIT), `author` (empty to `Peter Cummings <peter@linuxguard.io>`)
- **package.json moved dependency:** `tsx` (devDependencies to dependencies)
- **Deleted file:** `tsconfig.json.bak`

(03-02 separately produces the expanded README sections.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add MIT LICENSE file</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/03-publish-docs/03-PATTERNS.md (the `LICENSE` section gives the exact standard MIT text to use, copyright line "Copyright (c) 2026 Peter Cummings")
    - .planning/phases/03-publish-docs/03-CONTEXT.md (D-01: license = MIT, copyright "Peter Cummings"; Claude's Discretion covers exact year/format)
  </read_first>
  <action>
    Create `LICENSE` at repo root containing the standard MIT License text (per D-01). First line is `MIT License`. Copyright line: `Copyright (c) 2026 Peter Cummings`. Use the verbatim standard MIT body from the PATTERNS.md LICENSE section (permission grant, conditions, and the AS-IS warranty disclaimer). No repo URL or extra metadata — just the standard MIT text.
  </action>
  <verify>
    <automated>test -f LICENSE && head -1 LICENSE | grep -q '^MIT License$' && grep -q 'Copyright (c) 2026 Peter Cummings' LICENSE && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists at repo root.
    - First line is exactly `MIT License`.
    - Contains the line `Copyright (c) 2026 Peter Cummings`.
    - Contains the standard MIT permission grant and the AS-IS warranty disclaimer paragraph.
  </acceptance_criteria>
  <done>LICENSE file present at repo root with standard MIT text and "Copyright (c) 2026 Peter Cummings".</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json packaging metadata</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — license ISC, author empty, no files, no prepublishOnly, tsx in devDependencies)
    - .planning/phases/03-publish-docs/03-PATTERNS.md (the `package.json` section lists the exact 7 targeted field edits and the resulting dependencies/devDependencies blocks)
    - .planning/phases/03-publish-docs/03-CONTEXT.md (D-02 author value; D-03 tsx move; D-05 files allowlist + prepublishOnly; Claude's Discretion covers engines + keywords)
  </read_first>
  <action>
    Apply these targeted field edits to `package.json` (do NOT rewrite the whole file — change only these fields, preserving formatting of the rest):
    1. Change `"license": "ISC"` to `"license": "MIT"` (per D-01).
    2. Change `"author": ""` to `"author": "Peter Cummings <peter@linuxguard.io>"` (per D-02 — matches git commit identity).
    3. Add a `"files"` array set to `["index.ts", "src/", "README.md", "LICENSE", "tsconfig.json"]` (per D-05). Place it after the `"bin"` block.
    4. Add `"prepublishOnly": "tsc --noEmit"` to the `"scripts"` object (per D-05 — typecheck only, matches success criterion 2).
    5. Add a top-level `"engines"` field set to `{ "node": ">=18" }` (Claude's Discretion — `fetch`/`AbortSignal.timeout` require Node 18+).
    6. Replace `"keywords": []` with `["mcp", "sparkpost", "email"]` (Claude's Discretion — discoverability).
    7. Move `tsx` from `devDependencies` to `dependencies` (per D-03) — remove `"tsx": "^4.22.4"` from devDependencies and add it to dependencies (keep the same `^4.22.4` range). Resulting dependencies keys: `@modelcontextprotocol/sdk`, `tsx`, `zod`. Resulting devDependencies keys: `@types/node`, `typescript`.
    Do NOT add `repository`/`homepage`/`bugs` — deferred per D-02 (no git remote yet). Keep `name`, `version`, `description`, `main`, `type`, `bin`, and the `start`/`typecheck`/`test` scripts unchanged.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const a=[p.license==='MIT', p.author==='Peter Cummings <peter@linuxguard.io>', JSON.stringify(p.files)===JSON.stringify(['index.ts','src/','README.md','LICENSE','tsconfig.json']), p.scripts.prepublishOnly==='tsc --noEmit', !!p.engines&&p.engines.node==='>=18', !!p.dependencies.tsx&&!('tsx' in (p.devDependencies||{})), JSON.stringify(p.keywords)===JSON.stringify(['mcp','sparkpost','email']), !('repository' in p)&&!('homepage' in p)&&!('bugs' in p)]; if(a.every(Boolean)){console.log('PASS')}else{console.error('FAIL',a);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').license"` outputs `MIT`.
    - `node -p "require('./package.json').author"` outputs `Peter Cummings <peter@linuxguard.io>`.
    - `package.json` `files` array deep-equals `["index.ts", "src/", "README.md", "LICENSE", "tsconfig.json"]`.
    - `package.json` `scripts.prepublishOnly` equals `tsc --noEmit`.
    - `package.json` `engines.node` equals `>=18`.
    - `package.json` `keywords` deep-equals `["mcp", "sparkpost", "email"]`.
    - `tsx` is a key under `dependencies` and is NOT a key under `devDependencies`.
    - `package.json` does NOT contain `repository`, `homepage`, or `bugs` keys.
    - `npm run typecheck` exits 0 (config edits did not break the typecheck).
  </acceptance_criteria>
  <done>package.json has license=MIT, author set, files allowlist, prepublishOnly typecheck, engines.node, populated keywords, and tsx moved to dependencies; typecheck still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove tsconfig.json.bak and prove a clean minimal tarball</name>
  <files>tsconfig.json.bak</files>
  <read_first>
    - .planning/phases/03-publish-docs/03-CONTEXT.md (D-05 excludes tsconfig.json.bak; Claude's Discretion recommends deleting it; specifics note it is stray cruft)
    - .planning/phases/03-publish-docs/03-VALIDATION.md (phase gate: `npm publish --dry-run` exits 0 with the expected file list)
    - package.json (the `files` allowlist set in Task 2 drives tarball contents)
  </read_first>
  <action>
    Delete the stray `tsconfig.json.bak` from repo root (per D-05 recommendation; it is cruft, and the `files` allowlist already excludes it so it would not ship regardless). Then prove the packaging gates pass:
    - Run `npm pack --dry-run` to inspect the tarball file list. CRITICAL: `prepublishOnly` does NOT run on `npm pack --dry-run` — pack is for inspecting the file list only.
    - Run `npm publish --dry-run` to trigger the `prepublishOnly: tsc --noEmit` typecheck gate AND inspect the tarball. The published tarball must contain only: `index.ts`, the `src/` tree, `README.md`, `LICENSE`, `tsconfig.json`, and `package.json` (npm always includes package.json + README + LICENSE). It must NOT contain `test/`, `.planning/`, `.github/`, `tsconfig.json.bak`, `*.log`, or `.env`.
  </action>
  <verify>
    <automated>test ! -e tsconfig.json.bak && npm publish --dry-run > /tmp/sp_pub.txt 2>&1; status=$?; bash -c 'p=1; [ "'"$status"'" = 0 ] || { echo "FAIL: npm publish --dry-run exit non-zero"; p=0; }; for bad in test/ .planning/ .github/ tsconfig.json.bak .env; do grep -q "$bad" /tmp/sp_pub.txt && { echo "FAIL: tarball contains $bad"; p=0; }; done; for good in index.ts README.md LICENSE package.json; do grep -q "$good" /tmp/sp_pub.txt || { echo "FAIL: tarball missing $good"; p=0; }; done; [ "$p" = 1 ] && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json.bak` no longer exists at repo root.
    - `npm publish --dry-run` exits 0 (the `prepublishOnly: tsc --noEmit` gate passed).
    - The dry-run tarball listing includes `index.ts`, `README.md`, `LICENSE`, `package.json`, and the `src/` tree.
    - The dry-run tarball listing excludes `test/`, `.planning/`, `.github/`, `tsconfig.json.bak`, and `.env`.
    - `npm pack --dry-run` lists the same minimal file set (sanity check that pack and publish agree on contents).
  </acceptance_criteria>
  <done>tsconfig.json.bak removed; `npm publish --dry-run` exits 0 with a clean minimal tarball (source + README + LICENSE only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> npm registry | The published tarball crosses from the author's machine to the public registry. Only files in the `files` allowlist (plus npm's always-included set) cross. |

No runtime trust boundaries are added or changed by this plan — it edits packaging metadata and adds a LICENSE; it does not touch auth, input handling, sessions, access control, or crypto. (03-RESEARCH.md `## Security Domain`: Phase 3 adds no V2–V6 ASVS-relevant code.)

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | npm tarball contents | low | mitigate | `files` allowlist restricts the tarball to `index.ts`, `src/`, `README.md`, `LICENSE`, `tsconfig.json`; Task 3 asserts `test/`, `.planning/`, `.github/`, `.env`, `tsconfig.json.bak` are excluded so no secrets or planning notes leak. |
| T-03-SC | Tampering | tsx runtime dependency | low | accept | `tsx` is moved devDep -> dep, widening the published dependency surface. tsx is well-established (68M weekly downloads, created 2015, repo github.com/privatenumber/tsx) with no postinstall scripts [VERIFIED: npm registry, 03-RESEARCH.md Package Legitimacy Audit]. No new install task is introduced (tsx already present in lockfile), so no legitimacy checkpoint is required; risk accepted. |

No high-severity threats apply (`security_block_on: high`). Per 03-RESEARCH.md `## Security Domain`, Phase 3 adds no authentication, session, access-control, input-validation, or cryptography code; the MIT LICENSE and npm metadata carry no security risk, and `prepublishOnly` typecheck is a quality gate, not a security control.
</threat_model>

<verification>
- `npm publish --dry-run` exits 0 and the tarball listing is the minimal expected set (source + README + LICENSE + tsconfig + package.json); no `test/`, `.planning/`, `.github/`, `tsconfig.json.bak`, `.env`.
- `node -p "require('./package.json').license"` outputs `MIT` and `LICENSE` first line is `MIT License`.
- `tsx` resolves under `dependencies`; `devDependencies` no longer lists it.
- `npm run typecheck` exits 0 (no behavior change to any tool).
</verification>

<success_criteria>
- ROADMAP success criterion 2 met: `package.json` has a `files` allowlist, a LICENSE exists, and `prepublishOnly` runs the typecheck.
- ROADMAP success criterion 3 met: `npm publish --dry-run` produces a clean, minimal tarball.
- PKG-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-publish-docs/03-01-SUMMARY.md` when done.
</output>
